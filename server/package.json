{
  "name": "chronicle-ui-server",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run build:ui",
    "build:server": "tsc",
    "build:ui": "cd ../ui && npm install && npm run build-only && cp -r dist ../server/dist/static",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/ws": "^8.5.9",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "chronicle": "*"
  }
}
